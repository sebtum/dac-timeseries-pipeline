# Assumptions & open unknowns

Two registers. An **assumption** is something we're treating as true without having proved
it. An **unknown** is something we know we don't know.

Rules:
- Every assumption needs a way it could be shown false. If you can't write that column, it's
  not an assumption, it's a guess — say so.
- An unknown that gets resolved is closed with the fact *and* the evidence.
- An unknown we decide to live with is promoted to an assumption. That conversion is review
  material: "here's what I didn't know, and here's what I did about it."
- ADRs cite these by id.

---

## Assumptions

| id | assumption | why we need it | how it would be invalidated | status |
|----|-----------|----------------|-----------------------------|--------|
| A-01 | The project is untimed and all of A–J should reach a runnable/clickable state. | Sets milestone scope. | Sebastian re-imposes a clock. | held (source: commit `3693904`) |
| A-02 | InfluxDB 3 **Core** is adequate, rather than the free-for-at-home Enterprise build. | ADR-0004; Core lacks historical query capability, single-series indexing and Enterprise's query-time retention enforcement. | The ~72h query span makes the multi-experiment overlay unworkable (U-05), or the retention story can't be demonstrated on Core. | held |
| A-03 | 9 months raw / 1 year of 1-minute rollups / unbounded for pinned ranges are defensible **defaults**, standing in for a conversation with the electrochemical lab manager and process owners that has not happened. The post-shutdown high-resolution window is the same kind of placeholder. | ADR-0011 needs concrete numbers to create databases with, and Core fixes retention at creation. | Anyone states how far back runs actually get re-opened, or how long after a shutdown the data stays interesting. Either answer replaces the default; the *tier shape* is unaffected. | held (source: ADR-0011). Deliberately a parameter of the init script, not a constant in it — so the plant policy and the local/demo configuration can differ without either claim becoming false. **Local value set 2026-08-31:** raw 1 year, against the 9-month plant policy. All six experiments clear it with margin — `EXP_004` (2026-03-02) would not reach the boundary until 2027-03-02, where 9 months locally would expire it silently around 2026-12-02 (C-06). |

## Open unknowns

| id | what I don't know | blocks? | how I'd find out | status |
|----|-------------------|---------|------------------|--------|
| U-01 | Whether timestamps across the four sources share a timezone convention. | blocks M2 | Compare per-source timestamp distributions against `experiments.json` start/end times. | open (flagged by `task/DATA_DICTIONARY.md`) |
| U-02 | What CI runs against: the raw CSV is gitignored, so a runner has no dataset unless it regenerates it, or a committed fixture slice is provided, or data-dependent tests stay local. | ~~blocks the M0 CI increment~~ → blocks M2 | Time the generator to see whether regenerating in CI is even viable; otherwise commit a fixture slice. | **narrowed** (ADR-0009) — the M0 increment is lint, format and dependency install, none of which reads the dataset, so it is unblocked and lands now. The question becomes live at M2's ingest smoke test, the first check that needs actual data. |
| U-03 | Whether any requirement in this project actually calls for a relational store. | no | Falls out of M1, when experiment metadata gets a home. | **resolved** — yes: experiment metadata is mutable (three of six experiments have null fields that get filled in later) and metadata in Influx tags can't be corrected without rewriting every point. Resolution: ADR-0005. |
| U-04 | How downsampling is actually implemented on InfluxDB 3 Core — a Processing Engine Python plugin, or an external scheduled job. | ~~blocks M1~~ → blocks M3 | Prototype both against one experiment; compare operational cost and testability. | open (created by ADR-0004). **No longer blocks M1** (2026-08-31, `plans/M1.md`): the rollup splits into a pure aggregation function — locus-independent and unit-testable — and a thin driver that queries `dac_raw` and writes `dac_rollup`. What a plugin or a cron job would differ on is only *what calls the driver*, which is decided at M3 alongside U-09, when the verdict the rollup is meant to consume actually exists. **Narrowed and conflicted (ADR-0011).** Preference stated: plugin, unless the logic outgrows it. Verified: Core *does* ship the Processing Engine with `every:`/`cron:` scheduled triggers, and a scheduled plugin can query history and write to another database. But ADR-0011 scopes the rollup to consume M3's Python pipeline verdict, and a plugin cannot read a verdict that never materialises into InfluxDB — which is U-09, open until M3. Second axis: a plugin's CI check is an integration test needing a running server with the engine enabled and a trigger installed, where an external job is a unit test, and U-02 bites harder on the former. Resolve by either materialising the verdict into InfluxDB or running the rollup where the verdicts already are. |
| U-05 | Whether the six-experiment overlay can be served within Core's ~72h single-query span, or needs one query per experiment, or a raised Parquet file limit. | blocks M5, M6 — **and now profiling** | Load two experiments months apart and try the overlay query both ways. | open (created by ADR-0004). **Escalated by ADR-0010:** profiling moved post-ingest into SQL and spans March–June 2026, so this is hit as soon as profiling runs rather than at M5. Earlier exposure of a known blocker, but the sequencing changed. |
| U-06 | How the Python pipeline handles data that outgrows RAM — chunking, per-experiment processing, or pushing aggregation into the database. | no (not at 520k rows), but it is the most predictable review challenge | Decide the strategy, then demonstrate it on a chunked run even though the sample doesn't require one. | open (created by ADR-0003) — **substantially answered.** ADR-0007 made duplicate detection O(1) via sort-then-compare-adjacent. ADR-0010 then moved the bulk of profiling into SQL and reduced the pre-ingest pass to O(1) streaming, so Python only ever sees bounded samples. What remains open is the M3 cleaning pipeline's own memory strategy. |
| U-07 | The win-policy for conflicting duplicates — which of two readings claiming the same instant is kept. | blocks M2 | Run the M0 duplicate census first: the counts of exact vs conflicting collisions, and whether the conflicting pairs differ in `quality`, determine whether a quality-preference rule is even applicable. | open (created by ADR-0007). **Constrained, not free:** ADR-0007 requires the rule be content-based, since encounter-order rules stop being deterministic under parallel or streaming ingest. |
| U-08 | The definition of "same" when comparing two values for the exact-vs-conflicting duplicate split — raw CSV text or parsed float, and with what tolerance. | blocks M2 | Extract the actual colliding pairs in the M0 census and inspect them: if collisions are byte-identical in the source text, the question is moot. | open (created by ADR-0007). Both errors are silent: exact equality misclassifies round-tripped identical readings as conflicts; a tolerance misclassifies genuinely different readings as duplicates. |
| U-09 | Where processed values, pipeline quality verdicts and exclusion reasons live — extra columns on the readings tables, separate `*_clean` tables, or computed at query time. A second, independent axis from the numeric/state split in ADR-0006. | blocks M3 | Falls out of M3 Discuss, once the per-signal handling policy exists and the shape of a "reason" is known. | open (created by ADR-0006) |
| U-10 | Whether `quality` stays a dense field on every reading or becomes a sparse exceptions table where absence means good. | no | The M0 census gives the non-GOOD count per signal (currently 330 of 520,005 overall). Decide with M3, alongside U-09. | open (created by ADR-0006). Note: **not** a storage-size question — Parquet already dictionary- and RLE-encodes a column that is 99.94% one value. Interval/run-length representation earns its place as a *derived* artifact for annotating Grafana, which is M3/M6, not the storage layer. |
| U-11 | Whether conflicting duplicates ever occur on the two categorical signals, and therefore whether `readings_conflicts` needs string columns or a second table. | blocks M2 | The M0 duplicate census, broken down by signal. | open (created by ADR-0007) |
| U-12 | Whether the M3 cleaning pipeline reads from InfluxDB or re-reads the raw CSV. Determines whether ingestion is a one-shot bootstrap or a component the rest of the system depends on, and what "raw preserved unchanged" means operationally. | blocks M3 | Decide with U-09 in M3 Discuss; the answer partly falls out of whether processed data lands in the same store. | open. **Leaning set by ADR-0010:** profiling now reads from InfluxDB, so a cleaning pipeline that re-read the CSV would be working from a different surface than the analysis that justified its rules. |
| U-13 | How InfluxDB 3 Core's admin token works on first container start — whether it can be captured non-interactively into a gitignored `.env`, or whether running without auth locally is the cleaner option. | blocks M1 (compose file) | Start the pinned image and read what it actually does; check the `influxdb3` CLI's token subcommands. | open (created by ADR-0009). **Half-settled:** the *policy* is decided — auth is enabled for local development too, the verified bootstrap flow is scripted, the token lands in a gitignored `.env`, and no secrets-management system is introduced at this stage. The *mechanism* is still unverified: ADR-0008 and ADR-0009 both state Core mints the token on first boot and that claim has still not been tested in this repo. Verification is the first M1 Execute step and must precede the compose file. |
| U-14 | Whether the Postgres metadata schema arrives via `docker-entrypoint-initdb.d` init SQL or a migration tool. | blocks M1 | Decide at M1; `ROADMAP.md:92` already asks for a migration that "runs clean", which constrains it. | **resolved** (2026-08-30) — versioned SQL files applied by a small Python runner with an applied-versions table, one transaction per migration (safe because Postgres has transactional DDL). Init SQL excluded because it runs only on an empty volume and silently no-ops otherwise. `experiments.json` is separate bootstrap data loaded by an idempotent upsert loader with per-field provenance. Replacement trigger for a real tool is written into the ADR. See [ADR-0012](decisions/ADR-0012-postgres-migrations-metadata-loader.md). |
| U-17 | Whether the plant/testbench runs continuously or only during experiments. Determines whether whole experiments are the unit of "interesting data" or only selected parts of them. | no (design already accommodates both) | Ask the process owners. For *this* dataset the question is settled — every row carries an `experiment_id`, all six runs are 7200 s with explicit start/end, so experiments are the whole population. | open (created by ADR-0011). **Neutralised rather than blocking:** deliverable H frames the CSV as a historian export and a historian is continuous by nature, so pins are keyed by **time range**, never by experiment identity, and `experiments.json` is one *producer* of pinned ranges rather than the definition of one. A 24/7 rig therefore adds producers instead of forcing a redesign. |
| U-15 | Whether the pre-ingest census is M2's ingest detector run in a mode that writes nothing, or a standalone script. | blocks the census | Decide before writing it. Trade: reusing the detector means the census exercises real ingest code but pulls M2 work into M0; a standalone script keeps the milestones clean but writes the detection logic twice. | **resolved** (2026-08-29) — reused, but scoped narrowly: the sort-then-compare-adjacent detection pass ADR-0007:90 already specifies is a single module. The M0 census calls it in a mode that writes nothing; M2 calls it for real and layers win-policy resolution (U-07/U-08) on top once those are settled. Detection and resolution stay separate layers so the census isn't blocked on U-07. See ADR-0007 addendum. |
| U-16 | Where post-ingest SQL profiling sits on the milestone board — a new milestone between M2 and M3, M0 reopening after M2, or folding into M3's opening phase. | blocks the board restructure, not the work | Decide before restructuring `ROADMAP.md`. Note that folding into M3 risks C-02 again: measurement and remediation inside one milestone is where they blur. | **resolved** (2026-08-29) — new milestone, M2.5 — Post-ingest profiling, inserted between M2 and M3. Chosen over reopening M0 (would walk a `Done` milestone backward through the phase state machine) and over folding into M3 (risks repeating C-02). See `ROADMAP.md`. |
